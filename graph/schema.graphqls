# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Note {
  id: ID!
  title: String
  text: String
  citation: String
}

input NewNote {
  title: String
  text: String!
  citation: String
}

type Mutation {
  createNote(input: NewNote!): ID!
}